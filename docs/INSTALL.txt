d3kod.restapi Installation
--------------------------

* Add ``d3kod.restapi`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        d3kod.restapi
    
* Re-run buildout, e.g. with:

    $ ./bin/buildout
